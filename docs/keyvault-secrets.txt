# ================================================================
# Azure Key Vault - Secrets (per environment)
# ================================================================
# 3 secrets per vault. Names are exact (lowercase + dashes).
# Vaults: duke-mje-dev-kv, duke-mje-stg-kv, duke-mje-prod-kv
#
# Auth model:
#   - Entra ID (Azure AD) SSO for end users. The API is a pure resource server:
#     it only validates Microsoft-signed tokens via public JWKS. No app secret.
#   - Managed Identity / DefaultAzureCredential for service-to-Azure access
#     (Key Vault, Storage, etc.). No keys/secrets needed in deployed envs.
#
# What stays in KV: third-party API keys & DB password.
#
azure-openai-api-key       = <env-specific Azure OpenAI key>
azure-openai-endpoint      = https://<env-specific>.openai.azure.com/
#
# ---- Connection strings (host + creds bundled, per env) --------------------
database-url               = postgresql://duke:<password>@<host>:5432/duke_db
#
# ---- Optional --------------------------------------------------------------
pgadmin-password           = <env-specific pgAdmin password>

[TRANSCRIPTION NOTE]
The screenshot visibly ends after the pgadmin-password entry. Any content beyond
that point is not visible in the supplied screenshot.
